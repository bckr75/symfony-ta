{% extends 'base.html.twig' %}

{% block title %}Tax Number{% endblock %}

{% block body %}
    <h1>Tax Number</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tax_number.id }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ tax_number.country }}</td>
            </tr>
            <tr>
                <th>Prefix</th>
                <td>{{ tax_number.prefix }}</td>
            </tr>
            <tr>
                <th>Tax</th>
                <td>{{ tax_number.tax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tax_number_index') }}">back to list</a>

    <a href="{{ path('app_tax_number_edit', {'id': tax_number.id}) }}">edit</a>

    {{ include('common/_delete_form.html.twig') }}
{% endblock %}
